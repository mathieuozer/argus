apiVersion: v1
kind: ConfigMap
metadata:
  name: argus-config
  namespace: argus
  labels:
    app.kubernetes.io/part-of: argus
data:
  ARGUS_ENV: "production"
  ARGUS_LOG_LEVEL: "info"
  ARGUS_LOG_FORMAT: "json"
  ARGUS_TENANT_ENFORCEMENT: "strict"
  ARGUS_NATS_STREAM: "ARGUS_TELEMETRY"
